{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run databases",
            "command": "docker-compose -f docker-compose.db.yml up --build -d",
            "type": "shell",
            "group": "build",
            "presentation": {
                "reveal":  "silent"
            },
            "problemMatcher": "$msCompile"
        },
        {
            "label": "stop databases",
            "command": "docker-compose -f docker-compose.db.yml down",
            "type": "shell",
            "group": "build",
            "presentation": {
                "reveal":  "silent"
            },
            "problemMatcher": "$msCompile"
        },   
    ]
}